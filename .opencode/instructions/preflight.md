DO NOT start coding if you are on `main`.

You MUST:
1. Check current branch
2. If on `main`, as for the user to create a new branch, so you can continue
3. Only then proceed with the task
4. NEVER commit anything!
